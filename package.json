{
  "name": "ev-station-data-pipeline-sample",
  "version": "1.0.0",
  "private": true,
  "description": "EV station data ingestion pipeline — PG for current state, ClickHouse for event history",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test --import tsx test/**/*.test.ts",
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "@clickhouse/client": "^1.10.0",
    "pg": "^8.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
